
import { Sale } from "../types";
import { products } from "./products";

export const sales: Sale[] = [
  // Removed the incorrect sale. The sales array is now empty by default.
  // This will cause no sales to appear in the reports until actual sales are made.
];
